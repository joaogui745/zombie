### Configuração

- **Build:** MinGW 32-bits (deve estar no PATH)
- **SDL2:** Configure `SDL_DIR` no makefile (padrão: `C:/SDL2`)

### Comandos Make

- **`make`:** Compila o projeto
- **`make clean`:** Remove arquivos compilados e objetos
- **`make run`:** Compila e executa a aplicação
